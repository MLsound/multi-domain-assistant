"""
Auth module — own implementation of registration, login and JWT-based
session management.

Deliberately hand-rolled (no fastapi-users / no auth-as-a-service):
the assignment forbids frameworks that resolve the full flow.
"""
